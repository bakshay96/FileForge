# ─────────────────────────────────────────────
# FileForge — Backend Dependencies
# Python 3.11+
# ─────────────────────────────────────────────

# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async HTTP / file handling
python-multipart>=0.0.9
aiofiles>=23.2.1

# Settings management
pydantic-settings>=2.2.1
pydantic[email]>=2.7.1
email-validator>=2.1.0

# MongoDB async driver
motor>=3.4.0
pymongo>=4.7.2

# Image Processing
Pillow>=10.3.0

# PDF Processing
PyMuPDF>=1.24.3

# MIME Type Detection (Cross-platform)
python-magic-bin>=0.4.14; sys_platform == 'win32'
python-magic>=0.4.27; sys_platform != 'win32'

# Authentication & Security
python-jose[cryptography]>=3.3.0
bcrypt>=5.0.0

# Background scheduling (auto-cleanup)
APScheduler>=3.10.4

# HTTP Client
httpx>=0.27.0
